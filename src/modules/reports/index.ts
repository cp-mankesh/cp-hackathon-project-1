/**
 * Org-level reporting — aggregates live in the calls module (`getDashboardStats`, etc.).
 * (`getDashboardStats`) and rendered on `/dashboard`.
 */
export { getDashboardStats, type DashboardStats } from "@/modules/calls/server/call.service";
