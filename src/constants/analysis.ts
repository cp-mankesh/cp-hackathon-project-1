/** Max retries per analysis batch operation in the worker. */
export const ANALYSIS_BATCH_MAX_RETRIES = 3;

/** Base delay for exponential backoff between retries (ms). */
export const ANALYSIS_RETRY_BASE_MS = 500;
